# Examples

```bash
cd examples
python 01_hmba_response_matrices.py
python 05_save_and_reload_sc.py
```

Generated files are written to `examples/model_RM/` and `examples/output/`.

Operational templates live in `examples/operation/`. Fill in
`examples/operation/interface.py` before using them with a machine or control
system.
